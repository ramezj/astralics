import * as React from 'react';

export function BlitzFeedback(props:{projectId:any}) {
    return (
        <>
            <p>BlitzFeedback Welcomes u from TSDX, {props.projectId}</p>
        </>
    )
}